import pandas as pd
from pathlib import Path

def calculate_generation(df: pd.DataFrame) -> pd.DataFrame:
    """Add total_generation = Solar + Wind column."""

def calculate_surplus(df: pd.DataFrame) -> pd.DataFrame:
    """Add surplus = total_generation - consumption."""
